extends lay.pug

block content
    style.
        .google {
            background-color: #dd4b39;
            color: white;
        }
        input,
        .btn {
            width: 100%;
            padding: 12px;
            border: none;
            border-radius: 4px;
            margin: 5px 0;
            opacity: 0.85;
            display: inline-block;
            font-size: 17px;
            line-height: 20px;
            text-decoration: none; /* remove underline from anchors */
        }
    h1 Phase2
    h2
    h3 Andre Mendes, 43320
    h3 Samuel Costa, 43552
    img(src='https://www.isel.pt/media/assets/default/images/logo-isel.png' alt='ISEL' style='display: block; margin-left: auto; margin-right: auto')
    form(action='/action_page.php')
        .row
            .col
                a.google.btn(href='/login')
                    i.fa.fa-google.fa-fw
                    |  Login with Google